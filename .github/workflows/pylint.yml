name: static analytics

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

permissions:
  contents: read

jobs:
  lint:
    name: Pylint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
<<<<<<< HEAD
          pip install -r requirements.txt
          pip install pylint pylint-django
=======
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pylint
>>>>>>> b77c2fc (Update project files and workflow)

      - name: Run pylint on tracked Python files
        run: |
<<<<<<< HEAD
          pylint --load-plugins pylint_django crm cpq_project manage.py
=======
          pylint --disable=R,C0114,C0115,C0303 $(git ls-files '*.py')

  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
>>>>>>> b77c2fc (Update project files and workflow)
